name: ci
on:
  pull_request:
    branches:
      - 'main'
jobs:
  build-and-push:
    name: Build and push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      #      - name: Prepare
      #        id: prep
      #        run: |
      #          TAG=$(echo $GITHUB_SHA | head -c7)
      #          IMAGE="ghcr.io/Semantic-Society/neologism"
      #          echo ::set-output name=tagged_image::${IMAGE}:${TAG}
      #          echo ::set-output name=tag::${TAG}
      #- name: Check out the repo
      #  uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to GitHub Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push container image
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: user/app:latest
#          tags: |
#            docker.pkg.github.com/semantic-society/neologism/neologism2:latest
#            docker.pkg.github.com/semantic-society/neologism/neologism2:${{ github.sha }}
#            docker.pkg.github.com/semantic-society/neologism/neologism2:${{ github.ref }}
